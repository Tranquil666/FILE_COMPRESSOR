services:
  - type: web
    name: pdf-compressor
    env: node
    buildCommand: npm install
    startCommand: npm run production
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
